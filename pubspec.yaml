name: micro_app_login
description: A new Flutter package project.
version: 1.0.2
homepage:

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  micro_core:
    git:
      url: https://github.com/guilhermedaldim/micro_core.git
      ref: v1.0.0

  micro_commons_design_system:
    git:
      url: https://github.com/guilhermedaldim/micro_commons_design_system.git
      ref: v1.0.1

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
